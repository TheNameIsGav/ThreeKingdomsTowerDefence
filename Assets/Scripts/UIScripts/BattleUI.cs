using NUnit.Framework;
using UnityEngine;

public class BattleUI : MonoBehaviour
{
  
  //GOAL: Make an API that can be controlled/filled by the game manager

  //These will be held by the game manager but listing them here for now
  private int health;
	private int currency;
  private int waveNum;
  private string territory;
  private List towerList;









    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }









}
